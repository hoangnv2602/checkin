/**
 * CheckinPlaceholder — Phase 0 marker.
 * Module logic sẽ implement ở Phase tương ứng (xem docs/issues/).
 */
export function CheckinPlaceholder() {
  return (
    <div className="rounded border border-border bg-muted/30 p-4 text-sm text-muted-foreground">
      <strong className="font-semibold text-foreground">checkin</strong>
      <span className="ml-2">module — Phase 0 placeholder</span>
    </div>
  );
}
