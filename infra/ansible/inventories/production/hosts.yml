---
# Production inventory — placeholder cho I-017/I-018.
# Sau staging smoke test xong sẽ tạo prod VPS riêng.

all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    environment: production
  children:
    app:
      hosts: {}
    db:
      hosts: {}
